name: Repository metadata (testbed caller)

on:
  workflow_dispatch:
    inputs:
      service_id:
        description: Service identifier
        required: true
        default: litellm
        type: string
      environment:
        description: Environment identifier
        required: true
        default: dev
        type: string
      handoff_id:
        description: Producer run ID that uploaded the verified handoff artifact
        required: true
        type: string
      artifact_name:
        description: verified-ap-handoff-<service>-<environment>-<handoff_id>
        required: true
        type: string

permissions:
  actions: read
  contents: read

jobs:
  app-token:
    runs-on: ubuntu-latest
    outputs:
      token: ${{ steps.mint.outputs.token }}
      slug: ${{ steps.mint.outputs.app-slug }}
      inst: ${{ steps.mint.outputs.installation-id }}
    steps:
      - name: Mint GitHub App installation token
        id: mint
        uses: actions/create-github-app-token@bcd2ba49218906704ab6c1aa796996da409d3eb1
        with:
          client-id: ${{ vars.GH_APP_ID }}
          private-key: ${{ secrets.GH_APP_PRIVATE_KEY }}
          owner: ${{ github.repository_owner }}
          repositories: |
            team-docs-testbed
            test_project_auto_add
          permission-contents: read
          permission-actions: read
          permission-administration: write
          skip-token-revoke: true

      - name: Same-job token length
        env:
          SAME: ${{ steps.mint.outputs.token }}
        run: echo "same-job token length ${#SAME}"

  debug-token:
    needs: app-token
    runs-on: ubuntu-latest
    steps:
      - name: Report minted token length (value never printed)
        env:
          MINTED: ${{ needs.app-token.outputs.token }}
          SLUG: ${{ needs.app-token.outputs.slug }}
          INST: ${{ needs.app-token.outputs.inst }}
        run: echo "token=${#MINTED} slug=${#SLUG} inst=${#INST}"

  repository-topics:
    needs: app-token
    uses: DS-Jerry-in-Taiwan/team-docs-testbed/.github/workflows/repository-topics-sync.yml@76be75f205c9480c6b589e9f23ffc557a6f69d9d
    with:
      service_id: ${{ inputs.service_id }}
      environment: ${{ inputs.environment }}
      handoff_id: ${{ inputs.handoff_id }}
      consumer_run_id: ${{ github.run_id }}
      artifact_name: ${{ inputs.artifact_name }}
      team_docs_ref: 76be75f205c9480c6b589e9f23ffc557a6f69d9d
    secrets:
      team_docs_artifact_token: ${{ needs.app-token.outputs.token }}
      repository_topics_token: ${{ needs.app-token.outputs.token }}
